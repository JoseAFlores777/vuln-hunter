version: 2
updates:
  # Mantiene los SHA pineados de las GitHub Actions al dia con revision humana
  # (en vez de dejarlos pudrir tras pinearlos por SHA).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: chore(ci)
